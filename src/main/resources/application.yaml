spring:
  application:
    name: home_meal_hub
  datasource:
    url: jdbc:postgresql://localhost:5433/home_meal_hub
    username: postgres
    password: root
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 7777